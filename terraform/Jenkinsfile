pipeline {
    agent any
    environment {
        AWS_ACCESS_KEY_ID     = 'SUA_ACCESS_KEY_MINIO'
        AWS_SECRET_ACCESS_KEY = 'SUA_SECRET_KEY_MINIO'
    }
    stages {
        stage('init') {
            agent {
                docker {
                    image 'hashicorp/terraform:1.9.8'
                    args '-u 0:0 --entrypoint=""'
                    
 
                }
            }
            steps {
                dir('Lab/proxmox/terraform') {
                    sh '''
                      docker run --rm \
                      -v $(pwd):/workspace \
                      -w /workspace \
                      hashicorp/terraform:1.9.8 init -no-color
                    '''
                }
            }
        } 
    }  
}
