pipeline {
    agent any

    stages {
        stage('Init') {
            agent {
                docker {
                    image 'hashicorp/terraform:1.9.8'
                    args '--entrypoint=""'
                }
            }
            steps {
                sh 'terraform init -no-color'
            }
        }
    }
}